apiVersion: apps/v1
kind: Deployment
metadata:
  name: sushi
  labels:
    app: sushi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sushi
  template:
    metadata:
      labels:
        app: sushi
    spec:
      containers:
      - name: sushi
        image: xjohnfit/sushi:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5